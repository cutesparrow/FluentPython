#不管是哪种数据结构，它们都共用一套丰富的操作:迭代、切片、排序，还有拼接。
#list、tuple 和 collections.deque 这些序列能存放不同类型的数据。container
#str、bytes、bytearray、memoryview 和 array.array，这类序列只能容纳一种类型
# 容器保存的是引用，而扁平序列保存的是值
#扁平序列其实是一段连续的内存空间。由此可见扁平序列其实更加紧 凑，但是它里面只能存放诸如字符、字节和数值这种基础类型。
